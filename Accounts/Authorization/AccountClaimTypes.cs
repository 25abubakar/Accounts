namespace Accounts.Authorization;

/// <summary>Stable application identity claims stored in the auth cookie.</summary>
public static class AccountClaimTypes
{
    public const string StaffId = "staff_id";
}
